namespace Aexxa.CanvasCore
{
    public enum UILayerId
    {
        Background = 0,
        Screen = 100,
        Popup = 200,
        Overlay = 300,
        Toast = 400,
        Blocker = 900
    }
}
